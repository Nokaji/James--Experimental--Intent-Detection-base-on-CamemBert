# Présentation de l'expérimentation

## Ce repo est une expérimentation pour créer un model d'activation par intention pour James basé sur CamemBert (NLP)

# /!\ Ce code est généré par IA pour commencer mon apprentissage

avec python 3.13.X faites python -m venv .venv

puis ./.venv/Scripts/activate.ps1 (Sous windows) ou source ./.venv/Scripts/activate.ps1 (Sous Linux)

puis pip install -r requirements.txt

puis py index.py (le seul qui fonctionne, les autres sont des tests pour la version mobile, avec un model quantifié sous int8)
